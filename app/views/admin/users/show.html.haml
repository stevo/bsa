%h3 User
%p
  User: #{user.name}
%p
  Email: #{user.email if user.email}

= link_to t('links.user.rights'), edit_admin_user_rights_path(decorated_user), class: 'btn btn-primary', data: {colorbox: true}
= link_to(t('links.user.destroy'), admin_user_path(decorated_user), data: { confirm: 'Are you sure?' }, method: :delete, class: 'btn btn-danger') unless decorated_user.object == current_user
